# Performance report

Generated by `scripts/perf-report.ts` (`npm run perf:report`) from `docs/perf/perf-run.json` and `docs/perf/memory-run.json`.

## Setup

|  |  |
| --- | --- |
| Machine | Windows 10 Pro (Windows_NT 10.0.19045) |
| CPU | AMD Ryzen 9 5900X 12-Core Processor · 24 logical cores |
| RAM | 39.9 GB |
| Browser | chromium 153.0.8010.12 (headed, Playwright) |
| GPU | ANGLE (AMD, AMD Radeon RX 9060 XT (0x00007590) Direct3D11 vs_5_0 ps_5_0, D3D11) |
| Renderer | Pixi webgl |
| Viewport · DPR · canvas | 1920×1080 · 1 · 1920×1080 px |
| Build | commit `1335559` · production bundle (`vite build` → `dist-perf`, served by `vite preview`) |
| Match | `s180-i1` (180 s, a spawn every 1 s) · seed 1052498554 · custom true |
| Balance override | `player.maxHealth` = 100000 (default 100) |
| Run | 2026-09-19T04:30:08.369Z |
| Memory run | 2026-09-19T04:27:07.145Z · options s120-i3 · balance override `player.maxHealth` = 100000 (default 100) |

## Method

- `npm run perf` builds the production bundle into `dist-perf`, serves it with `vite preview` on port 4174 and runs `playwright.perf.config.ts` (Chromium, 1920×1080, DPR 1, headed by default so the real GPU is used; `PERF_HEADLESS=1` switches to headless). `npm run perf:report` re-renders this file from the JSON.
- `src/session/perfProbe.ts` is enabled only by `?perf=1` (read once in `main.tsx`). Each animation frame it stores the frame interval (the rAF timestamp via `document.timeline.currentTime`) in a preallocated `Float32Array` ring buffer, only while the match is `running` (loading, paused and ended frames are skipped). Every second of play it records a sample (FPS, p95 of that second, ships alive, projectiles, effects, `performance.memory` heap). When the match ends it builds the JSON report and downloads it through a Blob link.
- `scripts/perf.spec.ts` runs without `?test=1` (production mode: real clock, no hooks). It sets the options through local storage, raises the player's health through the dev balance so the ship survives the whole match (the run is therefore marked `custom`, which only excludes it from ranking), clicks Play and drives the ship with real key presses until the report downloads (≈ 3 min).
- Bot pattern: W held except 0.6 s of every 12 s; a 1.3 s turn every 5 s (right, right, left); Space held 3 s of every 4 s; Q and E held 0.4 s every 3 s, 1.5 s apart. Auto-resumes during the run: 0.
- `scripts/memory.spec.ts`: Menu → Play → sail and fire for 20 s → Pause → Main Menu; then HeapProfiler.collectGarbage ×2, Runtime.getHeapUsage and Performance.getMetrics. Cycle 0 is the menu before the first match.
- Targets (spec §18): mean ≥ 58 FPS, p95 frame time ≤ 20 ms, heap(5) ≤ heap(2) × 1.10.

## Results

| Metric | Value | Target | Verdict |
| --- | --- | --- | --- |
| Mean FPS (frames ÷ running time) | 239.98 | ≥ 58 | **PASS** |
| p95 frame time | 4.30 ms | ≤ 20 ms | **PASS** |
| p50 / p99 / max frame time | 4.20 / 4.30 / 12.50 ms |  |  |
| Display refresh (1000 ÷ p50; rAF follows vsync) | ≈ 238 Hz |  |  |
| Frames > 20 ms | 0 (0.00 %) |  |  |
| Frames > 33 ms | 0 (0.00 %) |  |  |
| Frames · running time | 43197 · 180.0 s |  |  |
| End | timeUp |  |  |
| Peak ships alive / projectiles / effects | 7 / 9 / 10 |  |  |
| JS heap start / end / peak | 12.0 MB / 15.3 MB / 17.0 MB |  |  |

Console errors during the run: none.

### Per 30 seconds

| Window | Mean FPS | Worst p95 (ms) | Ships avg / max | Max shots | Max effects | Heap at end |
| --- | --- | --- | --- | --- | --- | --- |
| 0–30 s | 239.9 | 4.30 | 6.3 / 7 | 8 | 8 | 13.2 MB |
| 30–60 s | 240.0 | 4.30 | 7.0 / 7 | 8 | 7 | 15.0 MB |
| 60–90 s | 240.0 | 4.30 | 6.9 / 7 | 8 | 7 | 16.7 MB |
| 90–120 s | 240.0 | 4.30 | 7.0 / 7 | 6 | 6 | 11.5 MB |
| 120–150 s | 240.0 | 4.30 | 7.0 / 7 | 6 | 5 | 12.1 MB |
| 150–180 s | 240.0 | 4.30 | 6.9 / 7 | 7 | 10 | 15.2 MB |

## Memory

| Cycle | Heap used (MB) | JSHeapUsedSize (MB) | DOM nodes | JS listeners | Documents | Canvases |
| --- | --- | --- | --- | --- | --- | --- |
| 0 (menu, before playing) | 4.23 | 4.23 | 157 | 196 | 3 | 0 |
| 1 | 7.72 | 7.72 | 224 | 208 | 3 | 0 |
| 2 | 7.98 | 7.98 | 224 | 208 | 3 | 0 |
| 3 | 8.21 | 8.21 | 224 | 208 | 3 | 0 |
| 4 | 8.38 | 8.38 | 224 | 208 | 3 | 0 |
| 5 | 8.62 | 8.62 | 224 | 208 | 3 | 0 |

Verdict: heap(5) ≤ heap(2) × 1.1 → 8.62 MB vs limit 8.78 MB (heap(2) = 7.98 MB): **PASS**

## Loading

The menu, Options and Captain's Log never download Pixi: `GameHost` sits behind `React.lazy` (spec §3). MSW's chunk loads before the first render because the mocks must be up before any query (spec §16). Sizes are from the `dist-perf` production build; gzip is computed by this script (level 6), the preview server itself does not compress.

| Group | Files | Raw | gzip |
| --- | --- | --- | --- |
| Entry: `index` + static imports (React, router, TanStack Query, Axios, data layer; every screen) | `index-DBrGhXYH.js`<br>`rolldown-runtime-hePW80VL.js` | 442.3 kB | 140.5 kB |
| Mocks: `browser` (MSW + fixtures; loaded at boot, before the first render) | `browser-R2Xbv4tR.js`<br>`rng-DfJ4Wucs.js` | 431.5 kB | 160.6 kB |
| Game: `GameHost` + static imports (Pixi core, render, session; downloaded on Play) | `GameHost-Ck1gFS6_.js`<br>`testControl-C-pbyKrG.js`<br>`perfProbe-DN0Pj0m8.js`<br>`BufferResource-5mcpIvpJ.js`<br>`GCManagedHash-umc2B15Y.js`<br>`Geometry-DwxvXD58.js`<br>`RenderTargetSystem-Cv8m-Kz1.js`<br>`Filter-CJITyfIO.js`<br>`getTextureBatchBindGroup-CEcctujA.js`<br>`GraphicsContext-DBpbDtIV.js`<br>`canvasUtils-D4q1eKNJ.js`<br>`Cache-BAz4lkWZ.js` | 573.4 kB | 174.5 kB |
| Pixi on demand: renderer stubs and extension chunks imported at `app.init` (only the used ones are fetched) | `BitmapFont-B4PQdx06.js`<br>`CanvasRenderer-BnfcBO3X.js`<br>`WebGLRenderer-DyCXIsOb.js`<br>`WebGPURenderer-ErL-ePpO.js`<br>`webworkerAll-Op6RyQ5f.js`<br>`init-CcF63aOD.js`<br>`browserAll-DXv6gx52.js` | 58.4 kB | 16.3 kB |
| Other: test API (`?test=1` only) and anything unreferenced | `testApi-BKqOXrTF.js` | 2.6 kB | 1.3 kB |
| Total JS | 24 files | 1508.2 kB | 493.2 kB |

## Observations

- **Build measured.** The run used the M7 working tree on top of commit `1335559`, which is the code of the M7 commit (the report's build line shows the parent commit because the changes were not committed yet; later runs label such a build `<sha>-dirty`).
- **Frame rate.** The reference display refreshes at 240 Hz and `requestAnimationFrame` follows vsync, so the probe's FPS is capped at 240, not 60. A mean of 239.98 FPS with p99 4.3 ms means every frame (fixed 60 Hz sim steps plus the Pixi render) finished inside a 4.2 ms budget, about four times under the 16.7 ms a 60 FPS target allows. On a 60 Hz display the same build is capped at 60 FPS. The single worst frame was 12.5 ms; no frame took longer than 20 ms.
- **Load over time.** The per-30 s table is flat from the first to the last window: the frame time does not drift as the match fills up. The load is bounded by design: at most 7 ships (the player plus the 6-enemy cap), so with a 1 s interval most spawns after the first few seconds are skipped at the cap, plus the balls in flight and short-lived effects. This is the heaviest match the Options allow.
- **Heap during play.** The JS heap saw-tooths between about 11 and 17 MB as minor GCs run, with no upward trend across the 3 minutes.
- **Memory across matches (PERF-03).** After every return to the menu the DOM node count (224), JS listeners (208), documents (3) and canvases (0) are identical, so nothing from a finished session stays attached. The heap after a forced GC still rises slightly (7.98 MB after cycle 2, 8.62 MB after cycle 5, +8 %, inside the 10 % limit). A 10-cycle diagnostic run with heap snapshots after cycles 2 and 10 ([memory-diagnosis.json](memory-diagnosis.json)) found that it grows in irregular steps of 0.02–0.31 MB per cycle (1.14 MB from cycle 2 to cycle 10), not by a fixed amount per match. About 1.07 MB of those 1.14 MB is V8 compiled code and type feedback (`InstructionStream`, `TrustedByteArray`, `FeedbackVector`, `Code`): the JIT optimising more functions as they run. About 12 KB is Chromium's own per-navigation performance entries (soft navigations, LCP records). App objects and arrays account for only a few KB. There is no per-match app state being retained.
- **Loading.** Pixi and the game code (≈ 175 kB gzip) are downloaded only when Play is pressed. The MSW chunk (≈ 161 kB gzip, most of it MSW itself plus the 24-captain fixtures) is loaded before the first render in every build, because the ranking and history mocks must be ready before any query. It is the price of running the mocks in production.
- **Limits of this evidence.** One machine and one browser (Chromium through Playwright), headed, at DPR 1 and 1920×1080 with a discrete GPU. Not measured: DPR 2–3 phones (the renderer caps `resolution` at 2 for that case), integrated GPUs, Firefox and Safari. The probe measures `requestAnimationFrame` intervals, not GPU time. `performance.memory` is Chromium-only and coarse. The seed is random per run and recorded in the JSON. The run raises the player's health to 100 000 through the dev balance so the ship survives all 180 s; that marks the match `custom` and changes no per-frame cost.
